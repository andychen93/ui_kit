# Argon UI Kit Phase 4

**Goal:** Table、Pagination、PageSelect、TreeSelect、QueryForm、ProTable、RowActions、StatusSwitch。PageSelect 按 rbac 交互（输入即搜索、防抖 300ms、value=整条 record）。

**Paging:** `@argon-kit/core` 的 `PageQuery` / `PageResult`，业务侧剥掉 `{ code, data }`。
