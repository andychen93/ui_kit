// @argon-kit/styles' "." entry (src/index.css) already @imports every
// individual stylesheet (tokens/button/form/overlay/table/layout/display/
// feedback/vendors), so a single import here is sufficient.
import '@argon-kit/styles';

// Import the playground
import './playground';
